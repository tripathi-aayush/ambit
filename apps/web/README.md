# Orion — web

Next.js 16 (App Router) frontend for Orion. See the [root README](../../README.md) for what Orion is and how to run the full stack.

```bash
npm install
npm run dev   # localhost:3000, expects services/core running on localhost:8000
```
