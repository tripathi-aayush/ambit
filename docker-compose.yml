services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: ambit
      POSTGRES_PASSWORD: ambit
      POSTGRES_DB: ambit
    ports:
      - "5433:5432"
    volumes:
      - ambit_pgdata:/var/lib/postgresql/data
      - ./infra/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ambit"]
      interval: 5s
      timeout: 3s
      retries: 10

  opa:
    image: openpolicyagent/opa:1.0.0
    command: ["run", "--server", "--addr", "0.0.0.0:8181", "/policies"]
    volumes:
      - ./infra/opa/policies:/policies
    ports:
      - "8181:8181"

  core:
    build: ./services/core
    env_file:
      - ./services/core/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://ambit:ambit@postgres:5432/ambit
      OPA_URL: http://opa:8181
      REPOS_DIR: /data/repos
    volumes:
      - ambit_repos_data:/data/repos
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      opa:
        condition: service_started

  sandbox:
    build: ./infra/sandbox
    privileged: true
    volumes:
      - ambit_sandbox_docker:/var/lib/docker
      # Phase 7: bind (not named volume) so the host path matches what
      # core writes to directly during local dev (core runs as a bare
      # uvicorn process, not containerized, in this workflow) -- both see
      # the exact same directory tree, no sync step needed. Nested DinD
      # resolves `docker run -v` source paths against this container's own
      # filesystem, which is why this must be a real bind mount and not a
      # named volume the inner daemon can't see into.
      - ./services/core/.data/plans:/plans

volumes:
  ambit_pgdata:
  ambit_sandbox_docker:
  ambit_repos_data:
